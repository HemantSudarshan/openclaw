## 2026-06-17 - Replaced insecure Math.random() with generateUUID()\n**Vulnerability:** Weak random number generation using Math.random() in attachment ID creation.\n**Learning:** The project has specific UUID utilities (ui/src/ui/uuid.ts) that should be preferred over Math.random() for security contexts in frontend code, especially since Math.random() is predictable.\n**Prevention:** Use existing secure random utilities provided in the project.
